---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: systems.synadia.nauth.io
spec:
  group: synadia.nauth.io
  names:
    kind: System
    listKind: SystemList
    plural: systems
    singular: system
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.systemId
      name: SystemID
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: System is the Schema for the Synadia Cloud system connection.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SystemSpec defines the desired state of System (Synadia Cloud
              connection).
            properties:
              apiCredentialsSecretRef:
                description: APICredentialsSecretRef references the Secret containing
                  the Bearer token for the Synadia API.
                properties:
                  key:
                    description: Key in the Secret; when empty, implementation-specific
                      default is used.
                    type: string
                  name:
                    description: Name of the Secret.
                    type: string
                  namespace:
                    description: Namespace of the Secret; when empty, defaults to
                      the System's namespace.
                    type: string
                required:
                - name
                type: object
              apiEndpoint:
                description: APIEndpoint is the Synadia Cloud API base URL (e.g. https://cloud.synadia.com).
                type: string
              reconcileInterval:
                description: |-
                  ReconcileInterval is how often accounts and users backed by this System are requeued for reconciliation.
                  Defaults to 5m if not set.
                type: string
              systemSelector:
                description: SystemSelector selects the system by name (e.g. "NGS").
                properties:
                  name:
                    description: Name of the system in Synadia Cloud (e.g. "NGS").
                    type: string
                required:
                - name
                type: object
              teamId:
                description: TeamID is the Synadia team identifier. Mutually exclusive
                  with TeamIDFrom.
                type: string
              teamIdFrom:
                description: TeamIDFrom loads the team ID from a ConfigMap or Secret.
                  Mutually exclusive with TeamID.
                properties:
                  key:
                    type: string
                  kind:
                    description: Kind is ConfigMap or Secret.
                    enum:
                    - ConfigMap
                    - Secret
                    type: string
                  name:
                    type: string
                  namespace:
                    type: string
                required:
                - key
                - kind
                - name
                type: object
            required:
            - apiCredentialsSecretRef
            - apiEndpoint
            - systemSelector
            type: object
            x-kubernetes-validations:
            - message: exactly one of teamId or teamIdFrom must be specified
              rule: has(self.teamId) != has(self.teamIdFrom)
          status:
            description: SystemStatus defines the observed state of System.
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              systemId:
                description: SystemID is the Synadia system identifier after resolution.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
